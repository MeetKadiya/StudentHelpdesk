# Copy to .env.local for local dev. Never commit real secrets (there aren't
# any here yet — this is a public runtime config value, not a secret).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
