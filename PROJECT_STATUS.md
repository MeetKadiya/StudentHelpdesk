# Project Status

This is a pointer file. The live, continuously-updated status document is:

`project-management/project_status.md`

(Kept at that path rather than duplicated here so there is exactly one
source of truth — see `TEAM_PROTOCOL.md`.)
