fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
pydantic-settings>=2.4
email-validator>=2.2
sqlalchemy>=2.0
asyncpg>=0.29
alembic>=1.13
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
# Pinned <4.1 (2026-08-10, found in a sandbox run this session): passlib's
# bcrypt backend detection reads bcrypt.__about__.__version__, which
# bcrypt>=4.1 removed. Unpinned, pip resolves bcrypt to 5.x and every
# hash_password()/verify_password() call in app/core/security.py crashes
# at runtime with "password cannot be longer than 72 bytes" (a red herring
# — the real error is upstream in passlib's version-detection, visible as
# a separate "error reading bcrypt version" warning right before it).
# Pre-existing since BACKEND-01/03 (not introduced this session) but only
# surfaces on an actual `pip install`, which no prior session's structural
# checks would have caught.
bcrypt<4.1
python-multipart>=0.0.9
redis>=5.0
celery>=5.4
minio>=7.2
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
# For a future DB-backed test fixture (TEST-01 / DEVOPS-04's CI) that uses
# SQLite instead of spinning up a real Postgres — verified working against
# the actual schema in a sandbox this session (see audit_log.py's JSON
# fix, added specifically so this becomes possible).
aiosqlite>=0.20
